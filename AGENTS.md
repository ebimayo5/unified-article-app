このリポジトリは、Codexとクロ（Claude Code）が並行して開発・運用するプロジェクトです。
以下のルールを必ず守ってください。

並行作業ルール（Codexとクロが同時に動く場合）

これまでは「どちらかが利用制限に達したときに引き継ぐ」前提だったが、
今後はCodexとクロが同時並行で作業することがある。以下を必ず守る。

1. 作業を始める前に、`HANDOVER_STATUS.md` 先頭の「現在作業中（ライブ状況）」を確認する。
   * もう一方のエージェントが直近30分以内に「作業中」を記録していて、
     対象ファイル・対象機能が重なる場合は、いきなり同じ箇所を編集しない。
     ユーザーに状況を伝えて確認するか、重ならない作業に切り替える。
2. 意味のある作業（ファイル編集、git commit、clasp push/deploy、
   自動投稿の開始・停止・再開）を始める直前に、「現在作業中」欄へ以下を書く。
   * エージェント名（Codex / クロ）
   * 開始時刻
   * やっていること（対象ファイル・タスクを具体的に）
   * 本番に影響する操作を行う予定か（clasp push/deploy、自動投稿の操作など）
3. 区切りがついたら（完了・中断どちらでも）「現在作業中」欄を空きに戻すか、
   完了内容に書き換える。長時間放置しない。
4. clasp push / clasp deploy / 自動投稿の開始・停止・再開の直前には、
   必ずもう一度「現在作業中」欄を読み直す。
   自分がさっき読んだ内容のままだと思い込まない。もう一方が並行で更新している可能性がある。
5. git commitはこまめに行う。大きな未コミット差分を長時間抱えたまま放置すると、
   もう一方のエージェントの変更とぶつかりやすくなる。作業の区切りごとにcommit→pushする。
6. 万一push時に競合（reject / non-fast-forward）が起きたら、force pushで押し切らず、
   `git pull --rebase` などで取り込んでから再度pushする。

作業開始時に必ず行うこと

1. リポジトリ直下の `HANDOVER_STATUS.md` を最初に読む。
   * 直前のエージェント（Codexまたはクロ）が何をしていたか、
     Gitの状態、自動投稿の状態、次に確認すべきことが書かれている。
   * 上記の「現在作業中（ライブ状況）」欄も必ず確認する。
2. `git status --short` と `git log -5` を実行し、
   `HANDOVER_STATUS.md` の記述と実際のリポジトリ状態が一致しているか確認する。
   食い違いがあれば、実際の状態を優先し、その旨をユーザーに報告する。
3. アーコンパネルで「処理中の記事を表示」を確認し、
   自動投稿・記事生成が処理中のまま止まっていないか確認する。
   同じ工程が20分以上進んでいない場合は、再開せず先に停止・キャンセル状態を確認する
   （2026-08-23に同様の見落としで55.47ドルのAPI消費事故が発生している）。
4. 上記1〜3を終えるまで、新しいOpenAI生成・clasp push・自動投稿の再開・
   git commitは行わない。

作業を終える/利用制限に達しそうなとき必ず行うこと

1. `HANDOVER_STATUS.md` を開き、以下を更新する。
   * 更新者・更新日時・切り替え理由
   * 直前まで何をしていたか（タスク・対象ファイル・目的）
   * Gitの状態（コミット済みか、未コミットの変更の内容、ブランチ、直近コミットハッシュ）
   * 自動投稿・本番デプロイの状態（動いているか止めたか、処理中の記事の有無、
     clasp pushやWordPress反映を行ったか）
   * 次のエージェントへの自由記述メモ
2. 可能であれば、作業中の変更はコミットするか、
   コミットしない場合はその理由と復元方法を `HANDOVER_STATUS.md` に明記する。
3. 自動投稿を意図的に止めた場合は、再開してよい条件を明記する。
4. 「現在作業中（ライブ状況）」欄が自分の名前のままになっていたら、空きに戻す。
